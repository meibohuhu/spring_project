package com.wendy.chat.controller;

// ExceptionHandler: means we have some kind of listener on the controllers, always detect and check if exception occurs within our controller.
// Do something within handler and send response to users(client)
public class ExceptionController {
}
